namespace SS3D.Systems.Animations
{
    /// <summary>
    /// Enum to track the different position a human can be into.
    /// </summary>
    public enum PositionType
    {
        Standing = 0,
        Sitting = 1,
        Proning = 2,
        Crouching = 3,
        Ragdoll = 4,
        RagdollRecover = 5,
        ResetBones = 6,
    }
}
