
.access-panel--hidden {
    display: none;
}

.access-gate-panel {
    flex-direction: column;
    align-items: center;
    justify-content: center;
    padding: 24px 20px;
    flex-grow: 1;
}

.access-gate-panel__slot {
    width: 52px;
    height: 36px;
    border-radius: 3px;
    background-color: var(--ss3d-diegetic-inset);
    margin-bottom: 12px;
    align-items: center;
    justify-content: center;
    box-shadow: inset 0 2px 6px rgba(0, 0, 0, 0.6);
}

.access-gate-panel__slot-clip {
    width: 100%;
    height: 100%;
    align-items: center;
    justify-content: center;
    overflow: hidden;
    position: relative;
}

.access-gate-panel__slot-line {
    width: 34px;
    height: 2px;
    background-color: var(--ss3d-border-subtle);
}

.access-gate-panel__scan-overlay {
    position: absolute;
    left: 2px;
    right: 2px;
    top: 2px;
    bottom: 2px;
    border-radius: 2px;
    background-color: var(--ss3d-feedback-info);
    opacity: 0.35;
}

.access-gate-panel__headline {
    font-size: 20px;
    letter-spacing: 1.6px;
    color: var(--ss3d-text-primary);
    -unity-text-align: middle-center;
    margin-bottom: 4px;
}

.access-gate-panel__sub {
    font-size: 11px;
    color: var(--ss3d-text-tertiary);
    -unity-text-align: middle-center;
    white-space: normal;
    margin-bottom: 12px;
}

.access-gate-panel__sub.font-terminal {
    color: var(--ss3d-text-tertiary);
}

.access-gate-panel__button .steel-button__control {
    min-width: 200px;
    min-height: 40px;
    padding: 10px 22px;
    font-size: 15px;
    letter-spacing: 1.2px;
    border-width: 1px;
    border-color: var(--ss3d-border-default);
    background-color: var(--ss3d-diegetic-panel-raised);
    color: var(--ss3d-text-primary);
    box-shadow: var(--ss3d-diegetic-shadow-button);
}

.access-gate-panel__button .steel-button__control:hover {
    background-color: #35373a;
    border-color: var(--ss3d-border-strong);
}

.access-gate-panel__button .steel-button__control:active {
    background-color: var(--ss3d-accent-rust-dim);
    color: var(--ss3d-text-on-accent);
    translate: 0 2px;
    box-shadow: none;
}

.access-gate-panel__button .steel-button__control:disabled {
    background-color: var(--ss3d-diegetic-panel-raised);
    color: var(--ss3d-text-tertiary);
    border-color: var(--ss3d-diegetic-border-subtle);
    box-shadow: none;
}
